-- Test 1: Valid CREATE TABLE statements
CREATE TABLE students (
    id int,
    name varchar(100),
    age int,
    gpa float
);

CREATE TABLE courses (
    course_id int,
    title varchar(255),
    credits int
);
